---
configs:
  clusters:
    - name: dev_plaintext
      bootstrapServers:
        - localhost:9092
      clientId: "abhishektest1"
      isEnabled: false
    - name: test_ssl_1WaySSL
      bootstrapServers:
        - localhost:9093
      isEnabled: false
      clientId: "abhishektest2"
      tlsDetails:
        enable2WaySSL: false
        trustedCerts:
          - "SHEPHERD_TRUSTCERT_1_PEM"
          - "SHEPHERD_TRUSTCERT_2_PEM"
        clientCert: "SHEPHERD_CLIENTCERT_PEM"
        privateKey: "SHEPHERD_CLIENTKEY_KEY"
        privateKeyPass: "SHEPHERD_CLIENTKEY_KEYPASS"
      config:
        - security.protocol: SSL
    - name: test2_sasl_plaintext
      bootstrapServers:
        - localhost:9093
      isEnabled: false
      clientId: "abhishektest3"
      config:
        - security.protocol: SASL_PLAINTEXT
        - sasl.mechanism: PLAIN
        - sasl.jaas.config: 'org.apache.kafka.common.security.plain.PlainLoginModule required username="admin" password="admin-secret"'
    - name: test3_sasl_plaintext_scram
      bootstrapServers:
        - localhost:9093
        - localhost:9094
      isEnabled: true
      clientId: "abhishektest4"
      config:
        - security.protocol: SASL_PLAINTEXT
        - sasl.mechanism: SCRAM-SHA-256
        - sasl.jaas.config: 'org.apache.kafka.common.security.plain.PlainLoginModule required username="admin" password="admin-pass"'
